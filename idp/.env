# ==========================
# Resource API Service
# ==========================

# Shared JWT verification with the IdP
JWT_SECRET=dev-secret-idp
JWT_ALG=HS256

# Context evaluation parameters
BUSINESS_HOURS_START=7
BUSINESS_HOURS_END=19
REGISTERED_DEVICE_IDS=mac-001,win-007,phone-123
TZ=Europe/Zurich

# Sensitive paths (prefix match: "/admin" covers "/admin/metrics")
SENSITIVE_PATHS=/export,/admin

# FastAPI host/port inside the container
HOST=0.0.0.0
PORT=8000
